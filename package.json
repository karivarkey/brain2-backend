{
  "name": "brain2",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "init": "bun scripts/init-db.ts",
    "embed": "bun test.ts embed",
    "clean": "sqlite3 chat.db \"DELETE FROM messages; DELETE FROM session_state;\" && sqlite3 memory_index.db \"DELETE FROM embeddings;\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@google/genai": "^1.42.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.6",
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
