info:
  name: Update memory
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{dev}}/api/memory/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
