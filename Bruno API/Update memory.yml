info:
  name: Update memory
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{dev}}/api/memory/:id"
  params:
    - name: id
      value: aleesa
      type: path
  body:
    type: json
    data: |-
      {
        "filename": "my-note",
        "metadata": {
          "type": "note",
          "tags": ["important"]
        },
        "content": "# My Note\n\nContent here"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
