info:
  name: Get all memory of people
  type: http
  seq: 5

http:
  method: GET
  url: "{{dev}}/api/memory?type=person"
  params:
    - name: type
      value: person
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
