info:
  name: Fetch Conversations
  type: http
  seq: 3

http:
  method: GET
  url: "{{prod-ip}}/chat/:chat_id"
  params:
    - name: chat_id
      value: conv_3
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
