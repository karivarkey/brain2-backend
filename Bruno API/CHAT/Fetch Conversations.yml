info:
  name: Fetch Conversations
  type: http
  seq: 3

http:
  method: GET
  url: "{{dev}}/chat/:chat_id"
  params:
    - name: chat_id
      value: conv_1772114483982
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
