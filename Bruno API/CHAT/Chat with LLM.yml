info:
  name: Chat with LLM
  type: http
  seq: 1

http:
  method: POST
  url: "{{dev}}/chat"
  body:
    type: json
    data: |-
      {
        "conversation_id": "conv_3",
          "message": "Hey jarvis , anything to note or remeber before going back to Chennai?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
