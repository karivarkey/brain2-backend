info:
  name: Chat with LLM
  type: http
  seq: 1

http:
  method: POST
  url: "{{dev}}/chat"
  body:
    type: json
    data: |-
      {
        "conversation_id": "conv_1",
          "message": "Hey jarvis, block my calendar for teusday , make sure the whole day I will be busy"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
