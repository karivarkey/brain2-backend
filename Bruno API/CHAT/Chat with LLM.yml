info:
  name: Chat with LLM
  type: http
  seq: 1

http:
  method: POST
  url: "{{prod-ip}}/chat"
  body:
    type: json
    data: |-
      {
        "conversation_id": "conv_3",
          "message": "Hey jarvis this is a test request to check if I can ping you to my prod env"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
