info:
  name: Fetch Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{dev}}/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
