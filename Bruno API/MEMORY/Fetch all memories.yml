info:
  name: Fetch all memories
  type: http
  seq: 5

http:
  method: GET
  url: "{{dev}}/api/memory"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
