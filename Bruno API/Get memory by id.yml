info:
  name: Get memory by id
  type: http
  seq: 7

http:
  method: GET
  url: "{{dev}}/api/memory/:user_id"
  params:
    - name: user_id
      value: aleesa
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
