info:
  name: Delete a memory
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{dev}}/api/memory/:id"
  params:
    - name: id
      value: aleesa
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
